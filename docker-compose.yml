version: '3.8'

services:
  ffl-suite:
    build: .
    image: ffl-suite:latest
    container_name: ffl-suite
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    environment:
      - FLASK_ENV=production
